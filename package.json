{
  "name": "topwebtool",
  "version": "1.0.0",
  "description": "Premium Utility Site Static Build Scaffolding",
  "scripts": {
    "build": "npx @tailwindcss/cli -i ./src.css -o ./styles.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  },
  "dependencies": {
    "fast-xml-parser": "^5.10.1",
    "ghost-cursor": "^1.4.2",
    "playwright": "^1.62.1"
  }
}
